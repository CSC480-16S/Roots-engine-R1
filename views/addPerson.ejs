<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Add Person</title>
    <link href='https://fonts.googleapis.com/css?family=Andada' rel='stylesheet' type='text/css'>
    <link href='https://fonts.googleapis.com/css?family=Roboto:500italic,300' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="styles/normalize.css">
    <link rel="stylesheet" href="styles/style.css">
    <link rel="stylesheet" href="styles/treeStyles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<header>
    <a href="/treeViewer" class="header-logo">
        <img id="logo-img" src="images/logo.png">
    </a>

    <div class="dropdown">
        <input class="hamburger" type="image" src="images/hamburger.png" onclick="myFunction()">
        <div class="dropdown-content" id="Dropdown">
            <a href="/acountInfo">Account Info</a>
            <a href="/treeViewer">Family Tree</a>
            <a href="map.ejs">Ancestory Distribution</a>
            <a href="/">Parliament Tree</a>
            <a href="mailto:rootsspammer@gmail.com">Contact Us</a>
            <a href="about.html">About</a>
            <a href="/logout">Log Out</a>
        </div>
    </div>
</header>

<div class="form wideForm">
    <div id="Profile Info">
        <form action="/createPerson" name="addPerson" method="post">
            <h1 id="addPersonHeader">Add Family Member</h1>
            <div class="leftForm">

                <input type="hidden" value="<%= id %>" name="id"/>
                <div class="field-wrap">
                    <label>First Name<span class="req">*</span></label>
                    <input name='firstName' type="text" maxlength="64"  required autocomplete="off" />
                </div>


                <div class="top-row">

                    <h2>Date of Birth <span class="req" >*</span></h2>
                    <div class="field-wrap">
                        <input name='dateOfBirth' type="date" placeholder="mm/dd/yyyy" required autocomplete="off" />
                    </div>

                    <div class="field-wrap">
                        <label><span class="req"></span></label>
                        <select name='gender' required>
                            <option value="" disabled selected>Gender</option>
                            <option value="male">Male</option>
                            <option value="female">Female</option>
                            <option value="trans">Transgender</option>
                            <option value="none">Prefer No Label</option>
                            <option value="diffent">Prefer Different Label</option>
                            <option value="nodisclose">Prefer Not to Disclose</option>
                        </select>
                    </div>
                </div>

                <div class="field-wrap">
                    <label>City of Birth<span class="req">*</span></label>
                    <input name='birthCity' type="text" maxlength="64"  required autocomplete="off" />
                </div>

                <div class="field-wrap">
                    <label>State of Birth<span class="req">*</span></label>
                    <input name='birthState' type="text" maxlength="64" required autocomplete="off" />
                </div>

                <div class="field-wrap">
                    <label>Country of Birth<span class="req">*</span> </label>
                    <input name='birthCountry' type="text" maxlength="128"  required autocomplete="off" />
                </div>

            </div>


            <div class="rightForm">

                <div class="field-wrap">
                    <label>Last Name<span class="req">*</span></label>
                    <input name='lastName' type="text" maxlength="64" required autocomplete="off" />
                </div>


                <h2>Date of Death </h2>
                <div class="field-wrap">
                    <input name='dateOfDeath' type="date" placeholder="mm/dd/yyyy" autocomplete="off" />
                </div>

            <div class="field-wrap">
                <label>City of Death</label>
                <input name='deathCity' type="text" maxlength="64"  autocomplete="off" />
            </div>

            <div class="field-wrap">
                <label>State of Death</label>
                <input name='deathState' type="text" maxlength="64" autocomplete="off" />
            </div>

            <div class="field-wrap">
                <label>Country of Death</label>
                <input name='deathCountry' type="text" maxlength="128" autocomplete="off" />
            </div>
  </div>
                <h2>Short Personal Biography</h2>
                <div class="field-wrap">

                    <textarea id="personalbio" onkeyup="counter(this, this.maxLength)" maxlength="5000" class="textareaclass" rows="4"></textarea>
                    <div align="right" id="num">5000 characters remaining</div>
                </div>

                <script>
                    function counter(txtarea, l){
                        var len = $(txtarea).val().length;
                        if (len <= l) $('#num').text((l - len) + ' characters remaining');
                    }
                </script>

                <input  type="file" onchange="previewFile()"><br>
                <img class="blank" src="" height="200"  alt="Image preview">

                <script>
                    function previewFile(){
                        var preview = document.querySelector('img.blank'); //selects the query named img
                        var file    = document.querySelector('input[type=file]').files[0]; //sames as here
                        var reader  = new FileReader();

                        reader.onloadend = function(){
                            preview.src = reader.result;
                        }

                        if (file) {
                            reader.readAsDataURL(file); //reads the data as a URL
                        } else {
                            preview.src = "";
                        }
                    }

                    previewFile();  //calls the function named previewFile()
                </script>


            <button type="submit" class="button button-block" id="wideButton"> Add Person</button>
        </form>
    </div>
</div>

<script src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
<script src="js/login.js"></script>

</body>
</html>
